{
  "name": "cli-caesar-cipher",
  "version": "1.0.0",
  "description": "the best cipher",
  "main": "./src",
  "dependencies": {
    "@types/node": "^14.11.2",
    "commander": "^6.1.0",
    "inquirer": "^7.3.3",
    "typescript": "^4.0.3"
  },
  "devDependencies": {},
  "scripts": {
    "encode": "npx tsc && node ./build -a encode -s 1",
    "decode": "npx tsc && node ./build -a encode -s 1",
    "build": "npx tsc"
  },
  "keywords": [
    "cipher"
  ],
  "author": "trit",
  "license": "ISC"
}
